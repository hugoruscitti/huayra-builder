import {
  repoLink
} from '../../../helpers/repo-link';
import { module, test } from 'qunit';

module('RepoLinkHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = repoLink(42);
  assert.ok(result);
});
