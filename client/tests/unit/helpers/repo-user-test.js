import {
  repoUser
} from '../../../helpers/repo-user';
import { module, test } from 'qunit';

module('RepoUserHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = repoUser(42);
  assert.ok(result);
});
