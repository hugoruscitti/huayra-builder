import {
  repoName
} from '../../../helpers/repo-name';
import { module, test } from 'qunit';

module('RepoNameHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = repoName(42);
  assert.ok(result);
});
